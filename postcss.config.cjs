module.exports = {
  plugins: {
    // Use the new package as the plugin, and pass the config path if necessary.
    // Often, it will automatically find tailwind.config.js or tailwind.config.cjs.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
};
